# 151-racing3 LEARNINGS

## 2026-08-20 携帯入力規則（左十字 / 右操作 / 下25%）

- 旧UIは左右42%×46%の巨大パッド＋ブレーキ左・ブースト中央で、プレイ面を食っていた。
- 携帯の入力規則を固定した。ドックはプレイ画面の下25%。左=十字キー（左右ステア、上=アクセル、下=ブレーキ）。右=GAS / BRAKE / BOOST。
- 打感は `pointerdown` + `scale(0.92)`（80ms）+ 短い `vibrate` + WebAudio のポチ音。アクセルは低音の踏み込み、ブレーキは短く硬い音、ブーストは高め。
- GAS と十字キー上は同時押ししても片方を離して切れないよう、入力はカウンタ（OR）で合成する。
- 速度メーターはドックと重ならないよう、タッチ表示時だけ `bottom: 25%` の上へ退避する。
- 実機の親指バランスは未検証。Chrome 構文チェックのみ実施。

## 2026-08-20 公開ゲート

- 出荷用に `SPEC.md` を追加。未参照 MP3 はコードから呼ばれていない（公開ブロッカーではない）。
- `index.html` は 1000行超のまま。分割は未実施。
- harness PASS（タップは START を canvas より前に置いて解消。通信量 0.08MB）。
